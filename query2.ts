import { db } from "./src/db/db"; import { admissionApplicationsV2 } from "./src/db/schema"; import { sql } from "drizzle-orm"; async function run() { const res = await db.select({ status: admissionApplicationsV2.paymentStatus, count: sql`count(*)` }).from(admissionApplicationsV2).groupBy(admissionApplicationsV2.paymentStatus); console.log("Admission Applications V2", res); process.exit(0); } run();
