import { db } from "./src/db/db"; import { transcriptRequests } from "./src/db/schema"; import { sql } from "drizzle-orm"; async function run() { const res = await db.select({ alatStatus: transcriptRequests.alatpayStatus, paystackStatus: transcriptRequests.paystackStatus, count: sql`count(*)` }).from(transcriptRequests).groupBy(transcriptRequests.alatpayStatus, transcriptRequests.paystackStatus); console.log("Transcript Processing", res); process.exit(0); } run();
